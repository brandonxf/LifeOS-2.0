# Solo se usa en `vite build` (no en `vite dev`): el APK compilado no tiene
# el proxy de vite.config.ts, así que necesita la URL absoluta del backend
# desplegado.
VITE_API_URL="https://lifeos-api-vh29.onrender.com"
